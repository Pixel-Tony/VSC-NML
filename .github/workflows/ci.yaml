# Build platform-specific versions of an extension and publish them
name: Publish new extension version to Visual Studio Marketplace

on:
  push:
    tags:
      - '*'

jobs:
  publish:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            dotnetRID: win-x64
            platform: win32-x64
          - os: macos-latest
            dotnetRID: osx-arm64
            platform: darwin-arm64
          - os: ubuntu-latest
            dotnetRID: linux-x64
            platform: linux-x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Publish .NET
        run: |
          dotnet publish "./src/server" --configuration Release --runtime ${{ matrix.dotnetRID }} --sc true --output server

      - name: Package VSIX
        run: npm install && node esbuild.js --production && npx vsce publish --target ${{ matrix.platform }}
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}